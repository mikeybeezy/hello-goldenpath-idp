apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

resources:
  - ../../base
  - namespace.yaml

# Override image to use ECR
# Tag is managed by argocd-image-updater (digest strategy)
images:
  - name: hello-goldenpath-idp
    newName: 593517239005.dkr.ecr.eu-west-2.amazonaws.com/hello-goldenpath-idp
    # newTag intentionally omitted - controlled by argocd-image-updater

patches:
  - patch: |-
      - op: add
        path: /spec/template/spec/imagePullSecrets
        value:
          - name: ecr-pull-secret
    target:
      kind: Deployment
      name: hello-goldenpath-idp

configMapGenerator:
  - name: hello-goldenpath-idp-config
    literals:
      - APP_ENV=local
      - LOG_LEVEL=debug
