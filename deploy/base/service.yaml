apiVersion: v1
kind: Service
metadata:
  name: hello-goldenpath-idp
  labels:
    app: hello-goldenpath-idp
    app.kubernetes.io/name: hello-goldenpath-idp
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: hello-goldenpath-idp
